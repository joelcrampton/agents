# agents

## Notes
- GPT models are trained with data up to a certain date
- If we ask questions about current events beyond that date it has no record of them
- Using tools, we can provide additional information/guidance
- For example a searchWeb() tool to allow the model to search the web when it needs external context
